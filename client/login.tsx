import React from 'react';
import { createRoot } from 'react-dom/client';

const Login = () => <div />;

const root = document.getElementById('content');
if (root) createRoot(root).render(<Login />);
